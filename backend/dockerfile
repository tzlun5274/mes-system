# 使用 Node.js 作為基礎映像
FROM node:14

# 設定工作目錄
WORKDIR /app

# 複製 package.json 和 package-lock.json 並安裝依賴
COPY package*.json ./
RUN npm install

# 複製其餘的應用程式檔案
COPY . .

# 開放 3000 端口，讓容器能被訪問
EXPOSE 3000

# 啟動應用程式
CMD ["npm", "start"]
